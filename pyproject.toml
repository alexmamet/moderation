[project]
name = "runpod-text-moderation"
version = "0.1.0"
description = "RunPod serverless text moderation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "runpod>=1.7.13",
    "torch>=2.0.0",
    "transformers>=4.30.0",
]
